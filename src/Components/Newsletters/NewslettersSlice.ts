import {createSlice, PayloadAction} from '@reduxjs/toolkit';

const initialState={}

const newslettersSlice=createSlice({
    name:'newsletters',
    initialState,
    reducers:{
     
    }
})

export const {}=newslettersSlice.actions;

export default newslettersSlice.reducer